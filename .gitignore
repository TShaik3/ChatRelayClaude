# Gradle
.gradle/
backend/build/

# Legacy javac build output (superseded by Gradle)
out/
test-out/

# Node / Vite
frontend/node_modules/
frontend/dist/

# OS
.DS_Store

# Host-specific deployment config (e.g. TAILSCALE_BIND) -- never the same value across machines
.env

# TLS cert + private key issued by `tailscale cert` -- never commit a private key
/certs/
