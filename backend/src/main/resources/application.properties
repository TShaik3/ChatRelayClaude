spring.application.name=chatrelay-backend
server.port=8080

spring.datasource.url=${CHATRELAY_DB_URL:jdbc:postgresql://localhost:5432/chatrelay_dev}
spring.datasource.username=${CHATRELAY_DB_USER:chatrelay}
spring.datasource.password=${CHATRELAY_DB_PASSWORD:chatrelay}
